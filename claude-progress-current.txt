## Session 118 - 2026-05-04 - WorkBetter partner-tier PLANNING

### Completed (this session)
PLAN-ONLY session. Zero code. Five commits on `wip-partner-tier`, all pushed.

- 5a79d05 docs(partner-tier): add tenancy decision, vocabulary, Slice 1 plan
- 9e77ba9 docs(partner-tier): expand with WorkBetter demo seed (Task G)
- 98610eb docs(partner-tier): demo (H), password change (C5), admin verify, claim_number (A2b)
- 654d70e docs(partner-tier): worker-form 'Who is your employer?' framing (H4)
- 40a165a docs(partner-tier): worker-form rule generalises to direct-employer path

### Artifacts produced
- docs/DECISIONS.md (appended) — Partner-Tier Tenancy Model entry. Locks
  the data model (partner role + access table + organizations.kind
  discriminator). Lists rejected alternatives + assumptions.
- CONTEXT.md (new at root) — domain vocabulary: organisation/employer/
  partner/partner-client/active-org/case tracks. Resolves preventative
  vs injury distinction (claim_number nullable on worker_cases).
- .planning/partner-tier/PLAN.md (new) — Slice 1 implementation plan:
  Tasks A-H (schema -> middleware -> auth -> picker -> header -> seed
  -> demo seed -> demo walkthrough). 12 verification steps. Slices 2-5
  listed as deferred.

### Driving customer
WorkBetter (real prospect, HR consultancy). Two dummy clients:
Alpine Health and Alpine MDF. Dev-only seed creds:
workbetter@workbetter.com.au / workbetter123.

### Key decisions locked
- Tenancy: partner role + access table + organizations.kind
- Storage: claim_number nullable on worker_cases (NULL = preventative,
  populated = injury). Schema must add this column (Task A2b).
- Worker-facing form: never asks "Company name" free-text — always
  "Who is your employer?" with pre-fill from case.organizationId.
  Generalises to direct-employer path too.
- Worker-facing email branding stays "Preventli" in MVP (deferred to
  Slice 2). Stated assumption — flag if WorkBetter requires sooner.

### In Progress
Nothing. Plan-only session, all artifacts committed. Build is next.

### Next Steps (NEXT SESSION)
1. /clear to reset context
2. Open this worktree
3. Paste the next-session prompt prepared at session 118 close
   (also reconstructable from session-4-preventli-web.md handoff)
4. /before-coding — should route to executing-plans
5. Confirm 4 stated assumptions with Paul
6. Execute Tasks A -> H per PLAN.md
7. Run all 12 verification steps with quoted evidence
8. Commit per task, push, open PR

### Blockers / open items
- Paul to save WorkBetter logo to attached_assets/workbetter-logo.png
  (claude can see image in chat but cannot extract bytes to disk).
  If absent, header is text-only — non-blocking.
- Confirm Resend SMTP env wired in dev (for Task H demo email).
  Falls back to console-logged link if not.

### Notes
- Drift watch: this session absorbed 7 scope additions during planning
  (white-label, intake form, GPNet admin idea, demo seed, password
  change, demo walkthrough, worker-form copy). Plan absorbed real
  adds, deferred speculative ones. Build session must hold scope
  HARD — anything new = "captured for next session, building Slice 1
  now."
- Future task #96 logged in
  ~/.claude/wsl-imported/projects/-mnt-d-dev/memory/tasks_outstanding.md:
  "Automated worker check-ins via WhatsApp/Viber/SMS"
- Session journal at
  ~/.claude/wsl-imported/sessions/session-4-preventli-web.md
  has full session-118 entry.

---

## Session 105 - 2026-03-05 (preserved from main)

### Completed
- Phase 11 & 12 commit (from session 104 carryover):
  - ContextualHelpSystem wired into CaseSummaryPage
  - DisputeStatus, CaseDispute added to schema
  - Related claims banner, dispute banner, mental health notice
  - RBAC: dispute status stripped from employer view
  - ContextualHelpSystem help topics for /cases and /employer routes

### Notes (legacy)
- Pre-existing TS errors only in discord-analytics.ts — not our code
- Build: exit code 0, 3797 modules
