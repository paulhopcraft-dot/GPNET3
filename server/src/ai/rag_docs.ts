// RAG disabled by default — scaffolding only.
// import { Pinecone } from "@pinecone-database/pinecone";
// import { OpenAI } from "openai";

export async function queryDocs(_q: string) {
  // Return empty docs until enabled
  return [];
}
