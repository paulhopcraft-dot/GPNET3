## Session 110 (Mar 22, 2026) — Compliance Milestone Clock

### Completed
- Built MilestoneClock card (case detail page): progress bar, ●/◑/○ markers, urgency colouring (red≤14d, amber≤30d)
- Built MilestoneBadge (CasesTable Days Off column): "→ Day 91" with countdown for ≤30d cases, Off Work only
- Wired MilestoneClock into CaseSummaryPage.tsx between status bar and tabs
- Fixed CaseSummaryPage queryKey: was "/api/gpnet2/cases" (50 cases), now "/api/gpnet2/cases?limit=200"
- Committed: d07910c — live tested in Playwright, renders correctly on FD-47007 (Day 60, 31d to Day 91)

### Known Issues / Next
- /api/gpnet2/cases?limit=200 is SLOW (~44-213s) — needs DB index on (work_status, date_of_injury)
- Always start server: NODE_OPTIONS="--max-old-space-size=4096" npm run dev
- Vite HMR does NOT work on WSL2/D: drive — restart server for every code change
- CaseDetailPanel.tsx is orphaned (has MilestoneClock import but isn't used anywhere)

### Server / Credentials
- Credentials: admin@gpnet.local / ChangeMe123!
- DB: postgresql://postgres:Whc%400102030405@localhost:5432/gpnet

---

## Session 109 (Mar 17, 2026) — v1 Testing Scenarios + Telehealth Booking Modal

### Completed
- Confirmed live site (https://gpnet3.onrender.com/) is working — login and dashboard functional
- Added TelehealthBookingModal component (client/src/components/TelehealthBookingModal.tsx)
  - Book telehealth appointment form with validation, file upload, confirmation state
  - Posts to /api/telehealth/booking
- Created comprehensive v1 testing scenarios doc (docs/V1_TESTING_SCENARIOS.md)
  - 18 modules covering all v1 features
  - Updated for live URL and current credentials
  - Includes bug report template and summary checklist

### In Progress
- Nothing in progress

### Next Steps
1. Wire TelehealthBookingModal into the UI (find where the "Book Telehealth" button should live)
2. Create /api/telehealth/booking endpoint on the server
3. Configure SMTP/Resend so emails actually send to telehealth providers
4. Run internal testing week (16–22 Mar) using docs/V1_TESTING_SCENARIOS.md
5. Fix pre-existing TS errors in agent-tools (currentCertificateEnd, clinicalStatusJson)
6. Beta client outreach (23–29 Mar)

### Blockers
- SMTP not configured — telehealth booking notifications won't send
- /api/telehealth/booking endpoint not yet created

### Test Credentials
- URL: https://gpnet3.onrender.com/
- Email: employer@symmetry.local
- Password: ChangeMe123!

### Active Worktrees
- main: /mnt/d/dev/gpnet3 [clean after commit]

---

## Session 106 (Mar 6–7, 2026) — Go-to-Market Planning + Production Readiness

### Completed

**Production readiness fix**
- Fixed fileUpload.ts logger errors (logger.info/warn → logger.storage.info/warn)
- Added `storage: createLogger("Storage")` to logger.ts
- Build: exit code 0, all 5 critical production issues now resolved

**Go-to-market docs (all committed)**
- `docs/LAUNCH_PLAN.md` — 6-phase launch plan, 27 tasks, target 13 Apr 2026
- `docs/V1_FEATURES.md` — v1 scope locked (staff-only, employer portal → v1.1)
- `docs/TARGET_CLIENT_PROFILE.md` — ICP: SME 50-200, construction/healthcare, HR Manager buyer
- `docs/PRICING_STRUCTURE.md` — two-track: pay-per-case ($349) + subscription ($799/$1,999/month)
- `.claude/v3/memory/launch-milestones.json` — machine-readable milestone JSON

**Worktree cleanup**
- Removed Windows path artifact files from feature/onboarding worktree (committed)
- All 3 worktrees clean

### In Progress
- Week 1 tasks remaining: website messaging, terms of service + privacy policy

### Next Steps (PRIORITY ORDER)
1. Choose hosting: Railway (recommended) — deploy to staging
3. Run db:push from Windows (RTW pathway fields) against staging DB
4. Run npx tsx server/seed-compliance-rules.ts from Windows against staging DB
5. Configure SMTP or Resend (emails silently failing)
6. Internal testing week (16-22 Mar)
7. Beta client outreach — 3-5 orgs, construction + healthcare (23-29 Mar)

### Blockers
- AI provider for production not decided yet (OpenRouter / Claude CLI / other) — TBD
- SMTP not configured → invites, password resets, notifications silently fail
- No hosting platform chosen yet → can't test staging

### Active Worktrees
- main: /mnt/d/dev/gpnet3 [clean]
- feature/onboarding: /mnt/c/Dev/worktrees/onboarding [clean]
- wip-deployment: /mnt/d/dev/gpnet3-deploy [clean — Control Tower + deployment stack]

### Production Readiness Score: 6.5/10
All 5 critical issues resolved. Gaps: no hosting, no API key, no SMTP.
Target 8/10 by end of Week 1 (15 Mar) after staging deployment.

### Key Decisions This Session
- v1 = Preventli staff only; employer portal = v1.1
- Pricing: per-case $349 for low-volume; subscription $799/month for 5+ concurrent claims
- ICP: SME 50-200 (construction, healthcare). Buyer: HR Manager.
- Launch date: 13 April 2026. Target: 3-5 paying clients.

---

## Session (Mar 5, 2026) — Control Tower + Production Deployment Stack

### Completed

**Control Tower Dashboard (full stack)**
- /api/control/overview, agents, ai, uploads, auth, alerts, performance — 7 admin-only endpoints
- /admin/control-tower — React page with 7 panels, 30s auto-refresh, lazy-loaded, AdminRoute protected
- MetricCard — reusable component (default/success/warning/danger variants)
- AdminSidebar updated with Control Tower nav item

**Alert Service (server/services/alertService.ts)**
- Types: ai_provider_failure, agent_failure_rate, database_failure, storage_failure, email_failure, health_check_degraded
- Channels: console (always), Slack (ALERT_SLACK_WEBHOOK), Telegram (ALERT_TELEGRAM_WEBHOOK)
- In-memory ring buffer (100 alerts) exposed at /api/control/alerts
- Health endpoint fires alerts on subsystem failures
- VALIDATED LIVE — alerts fired correctly when DB/AI missing

**Metrics Service (server/services/metricsService.ts)**
- RingBuffer class with avg(), p95(), max()
- Tracks: API request duration (1000 samples, per-route), agent job duration (500), AI latency (500)
- Express middleware auto-records every /api request
- callClaude() patched to record AI latency
- /api/control/performance back-fills agent durations from DB on first request

**Deployment Scripts (all executable, LF line endings)**
- scripts/validate-env.sh — validates all required vars with clear pass/fail output
- scripts/deploy-staging.sh — 7 phases: validate → build → start → health poll → migrate → tests
- scripts/deploy-production.sh — rollback tagging, confirmation prompt, auto-rollback on failure
- scripts/test-staging.sh — ~30 assertions across 8 sections
- scripts/load-test.sh — bash-only, configurable concurrency/duration, error rate threshold

**Config / Docs**
- .env.staging, .env.production.example, .env.example (alerting section added)
- .gitignore updated (.env.staging, .env.production)
- docs/preventli-launch-checklist.md
- docs/production-readiness-report.md

**Commit:** 990b994

### Known Pre-existing TypeScript Errors (NOT introduced by us)
- server/agent-runner/triggers.ts — cron namespace (TS2503)
- server/agents/agent-tools/*.ts — wrong property names (currentCertificateEnd, clinicalStatusJson)
- server/routes.ts — index type errors, implicit any

### Next Steps (priority order)
1. Provide real staging credentials → fill .env.staging → run deploy-staging.sh end-to-end
2. Fix pre-existing TS errors in agent-tools (currentCertificateEnd → correct field, clinicalStatusJson → clinical_status_json)
3. Discord webhook — needs bot token from Discord Developer Portal (server: 1468776337496997960, channel: 1478978031040200866)
4. Managed PostgreSQL — current docker postgres has no automated backups
5. Redis for rate limiter state — in-memory resets on restart

### Blockers
- No local PostgreSQL server (only client installed) — dev env DB-dependent tests can't run locally
- .env.staging has CHANGE_ME placeholders — deploy-staging.sh will reject until filled

### Key Architecture Notes
- workerCases.caseStatus = "open" | "closed" (NOT .status — caught this bug)
- audit_events.eventType: "user.login", "user.register", "user.login_failed"
- All Control Tower endpoints: authorize(["admin"])
- alertService: in-memory only, resets on restart — acceptable for single-instance
- metricsService ring buffers reset on restart; perf endpoint back-fills from DB (1h window)
- validate-env.sh: exits 1 on errors, warns (not fails) on missing SENTRY_DSN, SMTP

## Session 108 - 2026-03-12/13

### Completed
- Fixed Render Docker deployment (was completely broken — 7 sequential fixes)
  1. Dockerfile missing server/ in runner stage → added COPY
  2. vite.config.ts missing from runner → added COPY
  3. server/vite.ts serveStatic path wrong (dist vs dist/public) → fixed path
  4. tsconfig.json missing from runner (tsx can't resolve @shared alias) → added COPY
  5. transcripts/ directory permission denied → created in Dockerfile before non-root switch
  6. dist/public path fix (Vite outputs to dist/public not dist)
  7. Merge conflicts resolved throughout
- Preventli is now LIVE at https://gpnet3.onrender.com/ — login screen displays
- Started Uno bot (was dead, watchdog+startup.sh already in place)

### In Progress
- Render PostgreSQL setup — Paul is creating the DB on Render free tier
- Next: link DB URL as DATABASE_URL env var → redeploy → run db:push → run seed

### Next Steps
1. Create Render Postgres → copy Internal Database URL → set as DATABASE_URL env var on web service → redeploy
2. Run `npm run db:push` against live DB to create tables
3. Run `npm run seed` to create test accounts (employer@symmetry.local / ChangeMe123!)
4. Test login on https://gpnet3.onrender.com/

### Blockers
- No DATABASE_URL on Render yet — login will fail until DB is connected
- Discord bot (claude-code-discord) timing out on responses — needs investigation

### Notes
- Symmetry test creds: employer@symmetry.local / ChangeMe123! (after seed runs)
- All 8 Dockerfile fixes committed to main and pushed

---

## Session 4 (UAT) - 2026-03-19

### Completed
- Removed Call Support button from employer dashboard + summary dashboard (dd9bc99)
- Fixed 27 text contrast issues — light gray → readable slate throughout (c701e48)
- Built CaseActionPanel — persistent right sidebar on every coordinator case: last 5 actions, next recommended action with auto/manual badge, AI chat box (23cfe22)
- Rebuilt RTW Planner — full Option A/B plan creation wizard (3-step), email distribution to all parties, real recovery progress bar using actual weeks off work (b49ee47)
- Rebuilt employer case Summary tab as Command Centre — live "Week X off work" badge, Victorian compliance countdown (13/26/52-week milestones), 4 status cards (Compliance/Recovery/Next Action/Case Health), plain English risk, action feed; removed stale date/XGBoost jargon/"Generated using Template" (3667de7)
- Full live UX evaluation as coordinator — walked through Ethan Wells (69 weeks off, 455 days overdue), documented all failure points
- Analysed 488 anonymised Freshdesk tickets — identified 9 recurring failure patterns
- Generated: preventli-ux-report.md, freshdesk-pattern-analysis.md, preventli-redesign-proposal.md (all in /mnt/d/Downloads/)
- All 5 commits pushed and live on gpnet3.onrender.com

### In Progress
- Nothing — all work committed and pushed

### Next Steps (priority order)
1. Milestone clock — nightly job: Day 14 RTW plan required, Day 91 supervisor review, Day 182 compliance escalation, Day 364 annual review
2. Cases list → priority sort (Critical/Urgent/Routine/Monitoring) + quick action buttons per row
3. Cert expiry field — change from free text to structured date picker + 7-day warning + expiry alert
4. Contact attempt log — "attempted contact" button with date/method/outcome; 3 failures → auto-escalate
5. Fix 5 fake Checks tabs (Prevention/Injury/Wellness/Mental Health/Exit — all mock data)
6. Non-compliance detection + Victorian WorkCover Act references + insurer notification draft
7. New Case → two-pathway flow (WorkCover vs non-work-related)
8. Talk with an Expert — wire org + case context

### Blockers
- Unodisc Discord bot token invalid (unrelated to Preventli)
- Domain knowledge still needed from Paul:
  - The 6 check types and when to use each
  - Ideal Victorian RTW workflow step by step
  - New Case Pathway 2 fields (who/what/why/where/when/how)
  - Human-judgment non-compliance questions

### Notes
- CaseActionPanel is in coordinator view (/summary/:id) — NOT yet in employer view (/employer/case/:id)
- RTW Plans backend requires: medical cert with restrictions + RTW role with duties for full plan creation; fallback is status update
- 488 Freshdesk tickets analysed at /mnt/d/Downloads/freshdesk-anonymised.json
- Cert expiry free-text field is root cause of 20 cert stall cases — must become structured date
- RICH-001 Freshdesk thread is the gold standard case to emulate
- Render keep-alive cron running every 10 min
